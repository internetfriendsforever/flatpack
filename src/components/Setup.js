import React from 'react'

export default () => (
  <div>
    <h1>Flatpack AWS setup</h1>
    <p>
      Looks like you don’t have an AWS configured in your app.
      Follow the instructions to make or use a configuration on your AWS account.
    </p>
  </div>
)
